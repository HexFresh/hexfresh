export enum TaskCategory{
  SINGLE_CHOICE = 1,
  MULTIPLE_CHOICES,
  WRITTING,
  BINARY,
  ASSIGNMENT,
}

export enum TaskStatus{
  TODO,
  DOING,
  DONE,
}

export enum AttachmentStatus{
  SCANING,
  SCANNED
}